import 'package:flutter/material.dart';

class WidgetConstants {
  static const space = SizedBox(height: 24.0);
  static const smallDelay = Duration(milliseconds: 250);
  static const mediumDelay = Duration(milliseconds: 500);
}
